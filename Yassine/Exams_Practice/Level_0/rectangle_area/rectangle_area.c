int rectangle_area(int length, int width)
{
	return(length * width);
}

/*
#include <stdio.h>
int main(void)
{
	int length = 12;
	int width = 3;

	printf("length = %d\nwidth = %d\n"
			"area of rectangle: %d\n", length, width, rectangle_area(length, width));
	return (0);
}
*/